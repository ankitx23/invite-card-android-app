# Business Card

A single-screen Jetpack Compose app: a digital business card with a name, title, and tappable contact actions (share, email, phone).

Built while getting familiar with Compose — `Column`/`Row` layout, `Image`, and `Surface`/`MaterialTheme` for styling, all in one `MainActivity.kt`.
